name: ivs_maintenance_2
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # For DateFormat
  intl: ^0.17.0

  # Firebase (backend)
  cloud_firestore: ^2.2.2
  firebase_auth: ^1.4.1
  firebase_core: ^1.3.0
  editable: ^2.0.0
  tip_dialog: ^3.1.0
  provider: ^5.0.0
  searchable_dropdown: ^1.1.3
  flutter_datetime_picker: ^1.5.1
  firebase: ^9.0.1
  file_picker: ^3.0.3
  csv: ^5.0.0
  url_launcher: ^6.0.9
  clipboard: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true